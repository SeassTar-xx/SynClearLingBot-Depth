# SynClearDepth 固定测试集

本测试集在原始归档的完整 central directory 建立后、下载原始测试成员之前生成。固定版本为 `test_20pct_v1`，随机种子为 `20260718`，并按 scene configuration 分组以避免相邻帧泄漏。

详细配置、统计和分布比较分别见 `split_config.json`、`split_statistics.json` 与 `distribution_comparison.csv`。
