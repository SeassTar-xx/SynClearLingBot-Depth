# 本地模型目录

此目录存放从官方来源下载的模型权重。当前活动模型为 LingBot-Depth v0.5，预期路径为 `models/lingbot-depth-v0.5/model.pt`。

权重文件体积较大，始终被 Git 忽略。仓库仅保存模型 ID、来源、大小、SHA256 和激活记录，见 `ACTIVE_MODEL.txt` 与 `../manifests/lingbot_v05_checkpoint.json`。
